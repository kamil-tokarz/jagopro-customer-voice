(()=>{
  const form=document.getElementById('feedback-form');
  if(!form)return;
  const lang=document.body.dataset.lang||'pl';
  const sections=[...form.querySelectorAll('.section')];
  const stepButtons=[...document.querySelectorAll('.step-button')];
  const next=document.getElementById('next-btn'),prev=document.getElementById('prev-btn');
  const progress=document.getElementById('progress-fill'),count=document.getElementById('step-count');
  const mobileCount=document.getElementById('mobile-step-count'),mobileTitle=document.getElementById('mobile-step-title');
  const dots=[...document.querySelectorAll('.mobile-dots i')],validation=document.getElementById('validation');
  const body=document.getElementById('form-body-wrap'),success=document.getElementById('success');
  const contactFields=document.getElementById('contact-fields'),email=document.getElementById('email');
  const improvement=document.getElementById('improvement_priority'),improvementWrap=document.getElementById('improvement-reason-wrap');
  const npsReasonLabel=document.getElementById('nps-reason-label'),npsReason=document.getElementById('nps_reason');
  let current=0,furthest=0,submitted=false;
  const copy=lang==='pl'?{back:'Wstecz',next:'Dalej',send:'Wyślij opinię',sending:'Wysyłanie…',required:'Uzupełnij wymagane odpowiedzi w tej części.',invalidEmail:'Podaj poprawny adres e-mail albo wybierz „Nie” przy zgodzie na kontakt.',saveFail:'Nie udało się zapisać odpowiedzi. Spróbuj ponownie.',step:(n,total)=>`${n} z ${total}`,nps:{low:'Co przede wszystkim wpłynęło na tę ocenę?',mid:'Co moglibyśmy zrobić, aby zasłużyć na wyższą ocenę?',high:'Co najbardziej cenisz we współpracy z JagoPro?'},npsPh:{low:'Jeśli możesz, wskaż najważniejszy powód.',mid:'Wystarczy jedna konkretna rzecz.',high:'Wystarczy krótka odpowiedź.'}}:{back:'Back',next:'Next',send:'Submit feedback',sending:'Submitting…',required:'Please complete the required questions in this section.',invalidEmail:'Enter a valid e-mail address or choose “No” for follow-up contact.',saveFail:'We couldn’t save your response. Please try again.',step:(n,total)=>`${n} of ${total}`,nps:{low:'What was the main reason for this score?',mid:'What could we do to earn a higher score?',high:'What do you value most about working with JagoPro?'},npsPh:{low:'If you can, share the main reason.',mid:'One specific thing is enough.',high:'A short answer is fine.'}};
  const reduceMotion=()=>matchMedia('(prefers-reduced-motion: reduce)').matches;
  function showError(message,focusTarget){validation.textContent=message;validation.classList.add('show');if(focusTarget)focusTarget.focus({preventScroll:true});validation.scrollIntoView({behavior:reduceMotion()?'auto':'smooth',block:'nearest'})}
  function clearError(){validation.textContent='';validation.classList.remove('show')}
  function sectionValid(index=current){const section=sections[index];clearError();for(const group of section.querySelectorAll('[data-required-group]')){const name=group.dataset.requiredGroup;if(!group.querySelector(`input[name="${CSS.escape(name)}"]:checked`)){showError(copy.required,group.querySelector('input'));return false}}for(const field of section.querySelectorAll('input[required],select[required],textarea[required]')){if(field.closest('[hidden]'))continue;if(!String(field.value||'').trim()){showError(copy.required,field);return false}if(field.type==='email'&&!field.validity.valid){showError(copy.invalidEmail,field);return false}}return true}
  function syncSteps(){sections.forEach((section,index)=>section.classList.toggle('active',index===current));stepButtons.forEach((button,index)=>{const done=index<current||(index<furthest&&index!==current);button.classList.toggle('active',index===current);button.classList.toggle('done',done);button.disabled=submitted||index>furthest;if(index===current&&!submitted)button.setAttribute('aria-current','step');else button.removeAttribute('aria-current')});dots.forEach((dot,index)=>{dot.classList.toggle('active',index===current);dot.classList.toggle('done',index<current)});const title=sections[current].dataset.title||'';progress.style.width=`${((current+1)/sections.length)*100}%`;count.textContent=`${current+1}/${sections.length}`;mobileCount.textContent=copy.step(current+1,sections.length);mobileTitle.textContent=title;prev.disabled=current===0||submitted;prev.textContent=copy.back;next.textContent=current===sections.length-1?copy.send:copy.next}
  function scrollCard(){document.querySelector('.survey-card-v2')?.scrollIntoView({behavior:reduceMotion()?'auto':'smooth',block:'start'})}
  function goTo(index,{validateForward=true,scroll=true}={}){if(submitted||index<0||index>=sections.length||index>furthest)return;if(validateForward&&index>current&&!sectionValid(current))return;current=index;syncSteps();clearError();if(scroll)scrollCard()}
  stepButtons.forEach((button,index)=>button.addEventListener('click',()=>goTo(index,{validateForward:index>current})));
  prev.addEventListener('click',()=>{if(current>0)goTo(current-1,{validateForward:false})});
  function payload(){const data={};new FormData(form).forEach((value,key)=>data[key]=value);return{id:crypto.randomUUID?crypto.randomUUID():`fb-${Date.now()}`,submittedAt:new Date().toISOString(),language:lang,surveyVersion:(window.JagoProFeedbackConfig||{}).surveyVersion||'2.0',...data}}
  async function save(data){const cfg=window.JagoProFeedbackConfig||{};if(cfg.storageMode==='api'&&cfg.submitEndpoint){const response=await fetch(cfg.submitEndpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!response.ok)throw new Error(`HTTP ${response.status}`);return}const key='jagopro_feedback_v1';const rows=JSON.parse(localStorage.getItem(key)||'[]');rows.push(data);localStorage.setItem(key,JSON.stringify(rows))}
  next.addEventListener('click',async()=>{if(submitted||!sectionValid())return;if(current<sections.length-1){current++;furthest=Math.max(furthest,current);syncSteps();clearError();scrollCard();return}next.disabled=true;prev.disabled=true;next.textContent=copy.sending;try{await save(payload());submitted=true;stepButtons.forEach(button=>{button.disabled=true;button.classList.remove('active');button.classList.add('done');button.removeAttribute('aria-current')});dots.forEach(dot=>{dot.classList.remove('active');dot.classList.add('done')});progress.style.width='100%';body.style.display='none';success.classList.add('active');success.querySelector('h2')?.focus({preventScroll:true});success.scrollIntoView({behavior:reduceMotion()?'auto':'smooth',block:'center'})}catch(error){next.disabled=false;prev.disabled=false;next.textContent=copy.send;showError(copy.saveFail)}});
  form.addEventListener('submit',event=>event.preventDefault());
  form.addEventListener('change',event=>{if(event.target.matches('input[name="nps"]')){const score=Number(event.target.value);const optional=npsReasonLabel.querySelector('.optional');if(score<=6){npsReasonLabel.firstChild.nodeValue=copy.nps.low+' ';npsReason.placeholder=copy.npsPh.low}else if(score<=8){npsReasonLabel.firstChild.nodeValue=copy.nps.mid+' ';npsReason.placeholder=copy.npsPh.mid}else{npsReasonLabel.firstChild.nodeValue=copy.nps.high+' ';npsReason.placeholder=copy.npsPh.high}if(optional&&!npsReasonLabel.contains(optional))npsReasonLabel.append(optional)}if(event.target.matches('input[name="contact_permission"]')){const yes=event.target.value==='yes';contactFields.hidden=!yes;email.required=yes;if(!yes){document.getElementById('name').value='';email.value=''}}if(event.target===improvement){const none=improvement.value==='none';improvementWrap.hidden=none;if(none)document.getElementById('improvement_reason').value=''}clearError()});
  syncSteps();
})();
